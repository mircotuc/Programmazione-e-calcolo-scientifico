cmake_minimum_required(VERSION 3.20)
project(esercitazione10)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wshadow -Werror=vla")
# Creo il target per l'eseguibile principale del mio progetto software
find_package(Eigen3 REQUIRED) # per eigen

add_executable(testgcd testgcd.cpp)
target_link_libraries(testgcd PRIVATE Eigen3::Eigen)


add_test(NAME testmetodogcd COMMAND testgcd)

enable_testing()